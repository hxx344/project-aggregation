# 工作台与四模块代码精简记录

验证日期：2026-09-24。本次选择有导入闭包或等价输出证据的冗余，保留各仓库独立运行和部署方式。

## 整改范围

| 项目 | 检查基线 | 实际修改 |
| --- | --- | --- |
| Project Aggregation | `9faf919` | 标准摘要日期规范化只调用一次 `iso`，保持有效日期、无效日期及异常处理结果。 |
| ASTER 5X | `8f387e8` | 三处展示用 Unix 秒校验共用 `display-time.ts`；仍接受零、拒绝负数、非有限值和超出 Date 范围的值。显式允许 TypeScript 扩展名导入，使现有 Node 直接执行测试与前端构建共用实现。整改提交 `a9707cd`。 |
| Market Monitor | `91b40cf` | 删除 59 个不可达 UI 模板和孤立 `use-mobile`，共 7,146 行；删除 14 项无业务引用的直接依赖。整改提交 `41c6c04`。 |
| Asset Ledger | `f319fe0` | 删除 54 个不可达 UI 模板和孤立 `use-mobile`，共 6,662 行；删除 11 项无业务引用的直接依赖。整改提交 `d1ab308`。 |
| Gate CrossEx | `9320363` | 每个持仓的历史价差最大值计算一次，供两个统计指标复用，保留空值与负值口径。整改提交 `82ff15b`。 |

UI 清理使用 TypeScript AST 检查所有 Git 跟踪的 JS/TS 文件，覆盖相对导入、`@/` 别名、再导出、字面量动态导入和 `require`。以模板目录和孤立 Hook 以外的源文件为根，计算引用闭包；再检查字符串引用和非字面量加载。Monitor 的非字面量导入只用于框架 CLI 选择，不加载 UI 模板。

Monitor 保留 `table`、`tabs`；Asset 保留 `button`、`checkbox`、`dialog`、`select`、`sonner`、`table`、`tabs`，包括 `dialog → button` 的间接引用。原有 vendor CSS、实际使用的组件源码、React/Next、图表依赖均保留。逐个比较新旧锁文件，所有保留包的版本与完整性哈希均一致，无新增包；锁文件包条目分别减少 19 和 17 个。

修改限于模板、依赖声明和上述纯函数。接口、轮询和推送时序、缓存策略、资产计算、交易执行及部署脚本没有改动；未引入跨仓库共享包。各仓库原有未跟踪验收资料均保留。

## 回归证据

全部本地验证使用 Windows 原生运行时；未使用 WSL。Node 为 24.15.0，ASTER 生产构建使用项目已有的 Windows Node 22.23.2，避开已记录的 Node 24/Vinext 退出阶段问题。

| 项目 | 结果 |
| --- | --- |
| 工作台 | 115 项测试通过；TypeScript 检查与 Vite 生产构建通过。 |
| ASTER | 139 项前端测试通过；类型检查、lint、修改文件格式检查及 Vinext 生产构建通过。Python 后端未改动，本次未重跑后端整套测试。 |
| Monitor | 585 项测试中 579 通过、6 项按 Windows 平台跳过；类型检查、lint（0 错误、6 条原有警告）及 Next 生产构建通过。`build:linux` 命令在 Windows 原生 Node 执行，不代表本地 Linux 部署验收。 |
| Asset | 47 项测试、Next 生产构建及现有隔离生产冒烟通过；冒烟覆盖导入/备份、编辑与历史、账户持久化、重启及失败保留等路径。 |
| CrossEx | 215 项测试通过；类型检查与 Vite 生产构建通过。 |

合计 1,095 项测试通过、6 项平台跳过，0 项失败。另直接加载基线实现与整改实现，对同一输入进行 752 组输出/错误比较，全部一致：ASTER 时间显示与事件归并 440 组、工作台摘要日期 256 组、CrossEx 历史极值与抽样 56 组。包含零、负值、空值、非有限值、超大日期、时区字符串、空序列及超过抽样阈值的序列。核心整改前的工作台、CrossEx 和 ASTER 前端测试也分别通过。

Asset 的全量 `npm run lint` 尚未通过：它会扫描本机已有的 `.sites-runtime` 等生成目录；仅检查实际源码后，仍有 3 个基线错误：`app/aster-accounts-panel.tsx:19` 的 `react-hooks/purity`、`app/page.tsx:5` 的 `react-hooks/error-boundaries`、`lib/exchanges.ts:4` 的 `no-explicit-any`。这三个文件与基线完全一致，本次没有修改或屏蔽这些规则。不得将全量 lint 描述为通过。

## 页面与性能边界

使用整改前后实际 Next 生产构建比较 CSS，汇总所有生成 CSS 文件的内容：

| 项目 | 整改前 | 整改后 | 减少 |
| --- | ---: | ---: | ---: |
| Monitor | 234,510 字节 | 132,283 字节 | 43.6% |
| Asset | 160,008 字节 | 48,784 字节 | 69.5% |

删模板会影响 Tailwind 扫描，因此另外在同一 DOM 状态替换新旧 CSS，比较可见元素的 25 项计算样式，包括布局、宽高、间距、文字、边框、颜色和阴影。以下 8 个状态均为零差异、无整页横向溢出：Asset 桌面总览、编辑弹窗、地区下拉框、手机连接弹窗、手机已勾选账号弹窗；Monitor 手机原油页、桌面海力士页、手机海力士页。页面视口为 1440×1000 和 390×844，并人工检查保存的截图。

Asset 使用新建的隔离示例数据库，验证登录、手工资产保存及成功通知、导航、弹窗、下拉框和勾选；未配置真实账户。Monitor 复用现有 `tests/monitor-summary-refresh.browser.mjs`，使用固定时钟与行情夹具验证单次轮询、切页、断线旧值、隐藏/恢复及移动端；用于模拟不可用来源的 503 是预期夹具响应。

以上证据支持本次修改的等价行为与资源体积减小，不将构建时间或网络波动当作性能收益。服务器负载和真实网络端到端延迟未进行生产压测。Linux 验证由各仓库既有 GitHub Actions 执行；本次未部署用户服务器。

本地原始导入审计、锁文件/CSS比较、差分结果及测试日志保留于工作台 `output/code-simplification-20260924/`；截图保留于 `output/playwright/code-simplification/`。这些是验收产物，不纳入应用发布文件。
